const foo = "foo";

describe('', () => {
    it('', () => {
        const foo = "foo";
        const bar = 'bar';
        const baz = `baz`;
        const escapedQuotes = "'foo' \'bar\' baz";
        const manyEscapedQuotes = "'foo'' \\ \"bar\" baz";
    });
});
