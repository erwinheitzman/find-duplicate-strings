/* eslint-disable @typescript-eslint/no-unused-vars, no-undef */
const foo = "foo";

describe("", () => {
	it("", () => {
		const foo = "foo";
		const foobar = "f'o''o'bar";
		const bar = "bar";
		const baz = `baz`;
	});
});
